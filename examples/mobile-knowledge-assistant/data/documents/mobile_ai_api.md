# 移动端 AI 接入指南

## API Key 管理

移动端 App 不应该直接保存模型 API Key。客户端包可以被反编译，抓包工具也可能暴露请求内容。推荐做法是 App 调用自有服务端 API，由服务端保存模型密钥并调用大模型服务。

## 流式输出

移动端展示长回答时应优先使用流式输出。客户端收到片段后逐步追加到消息气泡，并在页面退出、用户点击停止或网络断开时取消请求。

## 引用来源

知识库问答应展示引用来源。移动端页面可以把引用来源折叠在答案下方，点击后展示文档标题、章节和原文片段。

