# 崩溃日志分析助手

## 输入内容

崩溃分析功能可以接收崩溃堆栈、设备型号、系统版本、App 版本和最近一次用户操作。服务端应去除用户隐私信息后再把上下文交给模型。

## 输出结构

建议输出崩溃摘要、可能原因、排查步骤、影响范围和置信度。客户端可以把排查步骤渲染为可勾选列表，方便工程师逐项验证。

## 安全要求

日志中可能包含手机号、邮箱、设备标识符或内部接口地址。上传前应在客户端或服务端进行脱敏，服务端日志也不应记录完整原始内容。

