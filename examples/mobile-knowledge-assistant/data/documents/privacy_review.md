# 移动端隐私与权限审查

## 相册与麦克风权限

多模态功能如果需要读取相册、拍照或录音，必须向用户解释用途。只在真正需要时申请权限，不要在 App 启动时一次性申请所有权限。

## 数据最小化

发送给大模型服务的上下文应遵循数据最小化原则。能在客户端或服务端本地完成过滤的字段，不应传给模型。

## 审计日志

服务端应记录请求 ID、用户 ID、功能入口、模型名称、Token 用量和错误码，但不应记录完整隐私文本。需要排查问题时，可使用脱敏后的摘要。

